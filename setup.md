1.clone project it contains 2 folders one frontend and second backend
2.go inside backend and do npm install and run node index/nodemon index
3.go inside frontend and do npm install and run npm start
